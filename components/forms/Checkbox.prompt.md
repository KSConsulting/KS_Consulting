20px box with a 6px radius; checked fills with the blue gradient and a soft glow.

```jsx
<Checkbox defaultChecked label="Aceito receber o diagnóstico por e-mail" />
```

Works controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`).
